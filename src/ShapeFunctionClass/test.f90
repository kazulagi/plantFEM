program main
    !use ShapeFunctionClass
    implicit none

    !type(FEmesh_)::shapefunc
!
    !integer i
!
    !shapefunc%NumOfNode = 1
    !shapefunc%NumOfOrder = 1
    !shapefunc%NumOfDim = 1
!
    !allocate(shapefunc%gzi(3))
    !shapefunc%gzi(1)=1.0d0
    !shapefunc%gzi(2)=0.0d0
    !shapefunc%gzi(3)=0.0d0
!
    !call GetShapeFunction(shapefunc)
!
    !do i=1,size(shapefunc%Nmat)
    !    print *, shapefunc%Nmat(i)
    !enddo
    !print *, shapefunc%ErrorMsg
    !print *, shapefunc%ierr
    
end program main